[tool:pytest]
addopts =
    --cov=src/xcloud_mcp
    --cov-report=term-missing
    --cov-report=html:htmlcov
    --cov-report=xml:coverage.xml
    --cov-fail-under=80
    -v
testpaths = tests
asyncio_mode = auto
